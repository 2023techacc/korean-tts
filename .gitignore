# Stray local backup archive, not part of the project.
/Allinone (2).zip

# Android / Gradle build outputs - regenerated by a build, not source.
# (Allinone (2)/ has its own .gitignore for the Python side.)
KoreanTTS-Android/**/build/
KoreanTTS-Android/.gradle/
KoreanTTS-Android/.idea/
KoreanTTS-Android/.kotlin/
KoreanTTS-Android/local.properties
KoreanTTS-Android/**/*.apk
KoreanTTS-Android/**/.cxx/
*.iml

# Web engine's bulky regression-test fixtures - regenerable from the Python
# engine (see docs/tests/*.mjs), not needed to serve the site itself.
docs/tests/audio_reference.json
docs/tests/audio_options_reference.json

# OS cruft
Thumbs.db
.DS_Store
