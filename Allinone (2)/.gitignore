.env
__pycache__/
*.pyc
_backup/
*.zip

# Discord bot (Pokemon RPG / music / school-info) - not part of the TTS work
# this repo now tracks. Left on disk to keep running locally, just untracked.
.env.example
README.txt
bot.py
main.py
pokemon_bot.py
pokemon.json
requirements.txt
voicetest.py
music/
pokemon_bot/
utils/
data/

# Built by PyInstaller (see BUILD_EXE.txt) - distributed via GitHub Releases,
# not committed; rebuild locally with the command in BUILD_EXE.txt.
KoreanTTS.exe
VoiceRecorder.exe
build_temp/
*.spec

# Per-machine state: GUI window settings, and the per-sample fine-tuning
# overrides gui.py's "고급 설정" tab saves (now under config/, not the top
# level - see korean_tts.OVERRIDES_SUBDIR/migrate_legacy_overrides). Both
# are local tuning data, not shared/versioned project config.
gui_settings.json
config/
