<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.KoreanTts" parent="Theme.MaterialComponents.DayNight.NoActionBar" />

    <!--
      No background, no animation, no status/nav bar dimming: the activity
      that reads a text selection out loud must look like nothing happened,
      since the whole point is that you never leave the app you were in.
    -->
    <style name="Theme.KoreanTts.Transparent" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
